apiVersion: v1
kind: ConfigMap
metadata:
  name: orchestrator-app
  namespace: agents
data:
  server.py: |
    import json
    import threading
    import time
    import uuid
    import urllib.parse
    import urllib.request
    from http.server import BaseHTTPRequestHandler, HTTPServer

    AGENT_BASES = [
        "http://researcher",
        "http://architect",
        "http://reviewer",
    ]

    JOBS = {}

    def get_json(url):
        with urllib.request.urlopen(url, timeout=30) as res:
            return json.loads(res.read().decode("utf-8"))

    def post_json(url, payload):
        data = json.dumps(payload).encode("utf-8")
        req = urllib.request.Request(
            url,
            data=data,
            headers={"Content-Type": "application/json"},
            method="POST",
        )
        with urllib.request.urlopen(req, timeout=300) as res:
            return json.loads(res.read().decode("utf-8"))

    def discover_agents():
        agents = []
        for base in AGENT_BASES:
            manifest = get_json(base + "/.well-known/agent.json")
            agents.append({
                "base": base,
                "manifest": manifest,
            })
        return agents

    def run_pipeline(job_id, task):
        JOBS[job_id]["status"] = "discovering"
        current = task
        outputs = []

        try:
            agents = discover_agents()
            JOBS[job_id]["agents"] = agents
            JOBS[job_id]["status"] = "running"

            for agent in agents:
                manifest = agent["manifest"]
                tool = manifest["tools"][0]
                name = manifest["name"]
                endpoint = tool["endpoint"]
                url = agent["base"] + endpoint

                JOBS[job_id]["current_agent"] = name
                result = post_json(url, {"input": {"task": current}})
                outputs.append((name, result))
                JOBS[job_id]["outputs"] = outputs
                current = result.get("output", current)

            JOBS[job_id]["status"] = "completed"
            JOBS[job_id]["current_agent"] = "done"

        except Exception as e:
            JOBS[job_id]["status"] = "failed"
            JOBS[job_id]["error"] = str(e)

    def index_page(task="", job_id=None):
        job_html = ""
        if job_id:
            job_html = f"""
            <p>Job accepted: <code>{job_id}</code></p>
            <p><a href="/result?id={job_id}">View result</a></p>
            """

        return f"""
        <!doctype html>
        <html>
        <head>
          <meta charset="utf-8">
          <title>Agent Orchestrator - Mini MCP</title>
          <style>
            body {{ font-family: sans-serif; margin: 40px; }}
            textarea {{ width: 720px; height: 120px; }}
            code {{ background: #eee; padding: 2px 4px; }}
          </style>
        </head>
        <body>
          <h1>Agent Orchestrator - Mini MCP</h1>
          <p>Discovery: <code>/.well-known/agent.json</code></p>
          <p>Invocation: <code>/tools/run</code></p>
          <form method="POST" action="/run">
            <textarea name="task">{task}</textarea><br>
            <button type="submit">Submit Job</button>
          </form>
          {job_html}
        </body>
        </html>
        """.encode("utf-8")

    def result_page(job_id):
        job = JOBS.get(job_id)
        if not job:
            return f"<html><body><h1>Job not found</h1><p>{job_id}</p></body></html>".encode("utf-8")

        refresh = ""
        if job["status"] in ["queued", "discovering", "running"]:
            refresh = '<meta http-equiv="refresh" content="3">'

        discovered = ""
        for agent in job.get("agents", []):
            manifest = agent["manifest"]
            tool_names = ", ".join([t["name"] for t in manifest.get("tools", [])])
            discovered += f"<li>{manifest.get('name')} - tools: {tool_names}</li>"

        rows = ""
        for name, result in job.get("outputs", []):
            rows += f"""
            <tr>
              <td>{name}</td>
              <td><pre>{result.get('input', '')}</pre></td>
              <td><pre>{result.get('output', '')}</pre></td>
            </tr>
            """

        error = ""
        if job["status"] == "failed":
            error = f"<p style='color:red;'>Error: {job.get('error', '')}</p>"

        return f"""
        <!doctype html>
        <html>
        <head>
          <meta charset="utf-8">
          {refresh}
          <title>Mini MCP Job Result</title>
          <style>
            body {{ font-family: sans-serif; margin: 40px; }}
            table {{ border-collapse: collapse; margin-top: 24px; }}
            th, td {{ border: 1px solid #ccc; padding: 8px 12px; vertical-align: top; }}
            th {{ background: #f0f0f0; }}
            pre {{ white-space: pre-wrap; }}
            code {{ background: #eee; padding: 2px 4px; }}
          </style>
        </head>
        <body>
          <h1>Mini MCP Job Result</h1>
          <p>Job ID: <code>{job_id}</code></p>
          <p>Status: <b>{job["status"]}</b></p>
          <p>Current Agent: <b>{job.get("current_agent", "-")}</b></p>
          <p>Task: {job.get("task", "")}</p>
          {error}

          <h2>Discovered Agents</h2>
          <ul>{discovered}</ul>

          <h2>Tool Outputs</h2>
          <table>
            <tr><th>Tool</th><th>Input</th><th>Output</th></tr>
            {rows}
          </table>

          <p><a href="/">Back</a></p>
        </body>
        </html>
        """.encode("utf-8")

    class Handler(BaseHTTPRequestHandler):
        def send_html(self, body):
            self.send_response(200)
            self.send_header("Content-Type", "text/html; charset=utf-8")
            self.send_header("Content-Length", str(len(body)))
            self.end_headers()
            self.wfile.write(body)

        def do_GET(self):
            parsed = urllib.parse.urlparse(self.path)
            if parsed.path == "/result":
                params = urllib.parse.parse_qs(parsed.query)
                job_id = params.get("id", [""])[0]
                self.send_html(result_page(job_id))
            else:
                self.send_html(index_page())

        def do_POST(self):
            parsed = urllib.parse.urlparse(self.path)
            if parsed.path != "/run":
                self.send_response(404)
                self.end_headers()
                return

            length = int(self.headers.get("Content-Length", 0))
            raw = self.rfile.read(length).decode("utf-8")
            params = urllib.parse.parse_qs(raw)
            task = params.get("task", [""])[0]

            job_id = str(uuid.uuid4())[:8]
            JOBS[job_id] = {
                "status": "queued",
                "task": task,
                "created_at": time.time(),
                "current_agent": "-",
                "outputs": [],
                "agents": [],
            }

            thread = threading.Thread(target=run_pipeline, args=(job_id, task), daemon=True)
            thread.start()

            self.send_html(index_page(task, job_id))

    HTTPServer(("0.0.0.0", 8080), Handler).serve_forever()
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator
  namespace: agents
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orchestrator
  template:
    metadata:
      labels:
        app: orchestrator
    spec:
      containers:
      - name: orchestrator
        image: python:3.12-slim
        command: ["python", "/app/server.py"]
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: app
          mountPath: /app/server.py
          subPath: server.py
      volumes:
      - name: app
        configMap:
          name: orchestrator-app
---
apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: agents
spec:
  type: NodePort
  selector:
    app: orchestrator
  ports:
  - port: 80
    targetPort: 8080
